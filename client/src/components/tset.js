const numbers = [1, 2, 3, 4, 5];
const numbers = numbers.map((number,i) => number +i);
console.log(doubled);

const numbers = [1, 2, 3, 4, 5];
for(let i=0;i<numbers.length;i++)
{
    numbers[i]=numbers[i]+i
}
console.log(doubled);